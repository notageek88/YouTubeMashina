name: Build Mac from private (two DMGs)

# Public-runner fallback for the private repository.
# The build body is synchronized with notageek88/youtube-mashina:
# .github/workflows/build-mac-tauri.yml
# Trigger baseline: private source 18e5902a1467f1b3056a2fecb0a3faf32110c9f1
on:
  push:
    branches: [main]
    paths:
      - .github/workflows/build-mac-from-private.yml
  workflow_dispatch:
    inputs:
      ref:
        description: Private branch, tag, or SHA to build
        required: true
        default: dev-last-v

env:
  PRIVATE_REF: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || 'dev-last-v' }}

jobs:
  resolve:
    runs-on: ubuntu-latest
    outputs:
      source_sha: ${{ steps.source.outputs.sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: notageek88/youtube-mashina
          ref: ${{ env.PRIVATE_REF }}
          token: ${{ secrets.PRIVATE_SRC_PAT }}
      - id: source
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

  build:
    needs: resolve
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runner: macos-15-intel
            machine: x86_64
            target: x86_64-apple-darwin
            deployment_target: "13.5"
            dmg: YouTube-MASHINA-macos-x64.dmg
          - arch: arm64
            runner: macos-15
            machine: arm64
            target: aarch64-apple-darwin
            deployment_target: "13.5"
            dmg: YouTube-MASHINA-macos-arm64.dmg
    runs-on: ${{ matrix.runner }}
    env:
      MACOSX_DEPLOYMENT_TARGET: ${{ matrix.deployment_target }}
      SOURCE_SHA: ${{ needs.resolve.outputs.source_sha }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: notageek88/youtube-mashina
          ref: ${{ needs.resolve.outputs.source_sha }}
          token: ${{ secrets.PRIVATE_SRC_PAT }}

      - name: Verify native runner
        run: |
          test "$(uname -m)" = "${{ matrix.machine }}"
          echo "source=$SOURCE_SHA arch=$(uname -m) target=${{ matrix.target }}"

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements-full.txt
            requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: ui/package-lock.json

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ui/src-tauri

      - name: Install build dependencies
        run: |
          brew install create-dmg llvm nasm
          python -m pip install --upgrade pip wheel setuptools
          python -m pip install --only-binary=av,numba,llvmlite -r requirements-full.txt "pyinstaller>=6.0"
          python -m pip install -r requirements.txt
          python -m pip install pytest

      - name: Test packaging helpers and macOS release contracts
        run: |
          python -m pytest tests/test_verify_macos_arch.py tests/test_package_extension.py tests/test_bundle_macos_ffmpeg.py tests/test_build_macos_ffmpeg.py tests/test_macos_workflow_contract.py tests/test_macos_dependency_floor.py -q
          python -m unittest tests/test_macos_release_contract.py tests/test_diagnostic_redaction.py tests/test_release_secret_scanner.py tests/test_runtime_config.py tests/test_llm_model_routing.py tests/test_runtime_security.py tests/test_extension_runtime.py -v
          node tests/native_clipboard.test.js
          node tests/license_actions.test.js

      - name: Bundle ffmpeg and build backend
        run: |
          bash scripts/build_macos_ffmpeg.sh data/bin
          test -f configs/config.py || cp configs/config.example.py configs/config.py
          bash scripts/build_backend.sh

      - name: Stage backend and build Tauri app
        run: |
          mkdir -p ui/src-tauri/resources
          find ui/src-tauri/resources -mindepth 1 -maxdepth 1 -exec rm -rf {} +
          cp -R dist/youtube-mashina-backend/. ui/src-tauri/resources/
          mkdir -p ui/src-tauri/resources/ui
          cp -R ui/extension ui/src-tauri/resources/ui/extension
          test -f ui/src-tauri/resources/ui/extension/manifest.json
          python tools/check_release_secrets.py ui/src-tauri/resources --allow-embedded-bug-bot
          cd ui
          npm ci
          python3 - <<'PY'
          import json
          from pathlib import Path
          path = Path("src-tauri/tauri.conf.json")
          config = json.loads(path.read_text())
          config["bundle"]["targets"] = ["app"]
          config["bundle"]["createUpdaterArtifacts"] = False
          config["bundle"]["macOS"]["minimumSystemVersion"] = "${{ matrix.deployment_target }}"
          config.setdefault("plugins", {}).setdefault("updater", {})["active"] = False
          path.write_text(json.dumps(config, indent=2) + "\n")
          PY
          npm run tauri:build -- --bundles app --target "${{ matrix.target }}"

      - name: Patch, sign and verify app
        env:
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
        run: |
          bash scripts/patch_macos_app.sh "${{ matrix.target }}"
          APP="ui/src-tauri/target/${{ matrix.target }}/release/bundle/macos/YouTube MASHINA.app"
          python scripts/verify_macos_arch.py --arch "${{ matrix.machine }}" --max-min-macos "${{ matrix.deployment_target }}" "$APP"
          python tools/check_release_secrets.py "$APP" --allow-embedded-bug-bot
          codesign --verify --deep --strict "$APP"

      - name: Create and verify DMG
        run: |
          APP="ui/src-tauri/target/${{ matrix.target }}/release/bundle/macos/YouTube MASHINA.app"
          mkdir -p release dmg-staging
          cp -R "$APP" dmg-staging/
          create-dmg --volname "YouTube MASHINA" --window-size 700 450 --icon-size 100 --app-drop-link 525 225 --no-internet-enable "release/${{ matrix.dmg }}" dmg-staging || hdiutil create -volname "YouTube MASHINA" -srcfolder dmg-staging -ov -format UDZO "release/${{ matrix.dmg }}"
          hdiutil verify "release/${{ matrix.dmg }}"
          printf '%s\n' "$SOURCE_SHA" > "release/${{ matrix.dmg }}.source-sha.txt"

      - uses: actions/upload-artifact@v4
        with:
          name: youtube-mashina-macos-${{ matrix.arch }}
          path: |
            release/${{ matrix.dmg }}
            release/${{ matrix.dmg }}.source-sha.txt
          if-no-files-found: error
          retention-days: 30

  extension:
    needs: resolve
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: notageek88/youtube-mashina
          ref: ${{ needs.resolve.outputs.source_sha }}
          token: ${{ secrets.PRIVATE_SRC_PAT }}
      - name: Package extension
        run: |
          mkdir -p release
          python scripts/package_extension.py ui/extension release/YouTube-MASHINA-extension.zip
          unzip -t release/YouTube-MASHINA-extension.zip
          printf '%s\n' "${{ needs.resolve.outputs.source_sha }}" > release/YouTube-MASHINA-extension.zip.source-sha.txt
      - uses: actions/upload-artifact@v4
        with:
          name: youtube-mashina-extension
          path: |
            release/YouTube-MASHINA-extension.zip
            release/YouTube-MASHINA-extension.zip.source-sha.txt
          if-no-files-found: error
          retention-days: 30
